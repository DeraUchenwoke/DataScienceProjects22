NOTE: Using pip version pip-21.3.1 (latest at to date)

python -m pip install packagename -> to install/uninstall/upgrade package names.
python -m pip install packagename
python -m pip uninstall packagename
python -m pip install --upgrade packagename

python -m pip install -r requirements.txt -> to install requirements.txt file.

